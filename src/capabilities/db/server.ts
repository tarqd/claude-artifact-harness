/**
 * db — STUB server. No routes and no websocket lanes yet.
 */
import type { ServerApps, ServerContext } from "../../server/types.ts";

export function routes(_apps: ServerApps, _ctx: ServerContext): void {
  // no routes
}
