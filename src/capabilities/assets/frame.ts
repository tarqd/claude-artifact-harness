/**
 * assets — STUB. Mounts nothing, so `claude.use("assets")` resolves `null`
 * and well-written pages degrade on their own. The assets slice replaces
 * this file.
 */
import type { FrameContext } from "../../frame/types.ts";

export function install(_ctx: FrameContext): void {
  // no namespace mounted
}
