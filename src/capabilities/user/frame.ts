/**
 * user — STUB. Mounts nothing, so `claude.use("user")` resolves `null`
 * and well-written pages degrade on their own. The user slice replaces
 * this file.
 */
import type { FrameContext } from "../../frame/types.ts";

export function install(_ctx: FrameContext): void {
  // no namespace mounted
}
